(()=>{let page=1,pages=1,csrf='';const $=id=>document.getElementById(id),grid=$('grid'),summary=$('summary'),q=$('q'),pageInfo=$('pageInfo');const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function auth(){try{const r=await fetch('../server/api.php?action=me',{cache:'no-store'}),j=await r.json();csrf=j.csrf||''}catch(e){}}async function load(){grid.classList.add('loading');summary.textContent='Ищу товары с отсутствующими или недоступными фотографиями…';try{const u=new URL('../api/photo-moderation.php',location.href);u.searchParams.set('page',page);u.searchParams.set('limit','12');if(q.value.trim())u.searchParams.set('q',q.value.trim());const r=await fetch(u,{cache:'no-store'}),j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error||`HTTP ${r.status}`);pages=j.pages||1;summary.textContent=`Нужно проверить: ${j.total}. Страница ${j.page} из ${pages}.`;pageInfo.textContent=`${j.page} / ${pages}`;$('prev').disabled=page<=1;$('next').disabled=page>=pages;if(!j.items.length){grid.innerHTML='<div class="empty">Товаров для модерации на этой странице нет.</div>';return}grid.innerHTML=j.items.map(card).join('');bind()}catch(e){summary.innerHTML='<span class="err">Не удалось загрузить очередь фотографий.</span>';grid.innerHTML='';console.error(e)}finally{grid.classList.remove('loading')}}function card(p){const c=(p.candidates||[]).map((x,i)=>`<button class="cand" type="button" data-url="${esc(x.image)}" data-id="${p.id}"><img src="${esc(x.image)}" loading="lazy" alt=""><small>${esc(x.source_title||'Вариант')}</small></button>`).join('');return `<article class="product" data-product="${p.id}"><h2>${esc(p.name)}</h2><div class="meta">${p.sku?`Артикул: ${esc(p.sku)} · `:''}${p.stock_qty!=null?`Остаток: ${p.stock_qty} · `:''}${esc(p.category_path||'')}</div><div class="cands">${c||'<div class="muted">Автоматических вариантов пока нет.</div>'}</div><div class="manual"><input class="manualUrl" placeholder="Или вставить прямую ссылку на фото"><button class="btn chooseManual" type="button">Поставить</button></div><div class="actions"><button class="btn save" type="button" disabled>Выбрать фото</button><button class="btn secondary clear" type="button">Оставить без фото</button></div><div class="status"></div></article>`}function bind(){document.querySelectorAll('.product').forEach(card=>{let selected='';card.querySelectorAll('.cand').forEach(b=>b.onclick=()=>{card.querySelectorAll('.cand').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');selected=b.dataset.url||'';card.querySelector('.save').disabled=!selected});card.querySelector('.save').onclick=()=>selected&&save(card,selected);card.querySelector('.chooseManual').onclick=()=>{const v=card.querySelector('.manualUrl').value.trim();if(v)save(card,v)};card.querySelector('.clear').onclick=()=>save(card,'')})}async function save(card,url){const st=card.querySelector('.status');st.textContent='Сохраняю…';try{const r=await fetch('../api/photo-moderation.php',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({product_id:Number(card.dataset.product),image_url:url})}),j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error||r.status);st.innerHTML='<span class="ok">Готово. Фото сохранено.</span>';setTimeout(load,500)}catch(e){st.innerHTML='<span class="err">Не удалось сохранить.</span>';console.error(e)}}$('search').onclick=()=>{page=1;load()};$('reset').onclick=()=>{q.value='';page=1;load()};q.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();page=1;load()}});$('prev').onclick=()=>{if(page>1){page--;load()}};$('next').onclick=()=>{if(page<pages){page++;load()}};(async()=>{await auth();await load()})()})();